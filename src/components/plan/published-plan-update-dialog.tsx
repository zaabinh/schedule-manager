"use client";

import { Dialog } from "@/components/ui/dialog";
import { Button } from "@/components/ui/button";
export function PublishedPlanUpdateDialog({ open, onOpenChange, onSave }: { open: boolean; onOpenChange: (value: boolean) => void; onSave: (website: boolean, email: boolean) => void }) { return <Dialog open={open} onOpenChange={onOpenChange} title="Lưu thay đổi kế hoạch đã công bố" description="Kế hoạch đã công bố vừa được thay đổi."><p className="text-sm font-medium">Bạn có muốn thông báo cho giáo viên?</p><div className="mt-4 space-y-2"><label className="flex items-center gap-3 rounded-lg border p-3 text-sm"><input id="notify-web" type="checkbox" defaultChecked/>Thông báo trên website</label><label className="flex items-center gap-3 rounded-lg border p-3 text-sm"><input id="notify-email" type="checkbox" defaultChecked/>Gửi email</label></div><p className="mt-3 text-xs text-slate-500">Bạn có thể bỏ chọn cả hai nếu chỉ sửa lỗi nhỏ. Hệ thống không tự động gửi.</p><div className="dialog-actions"><Button variant="secondary" onClick={() => onOpenChange(false)}>Hủy</Button><Button onClick={() => { const web = (document.getElementById("notify-web") as HTMLInputElement).checked; const email = (document.getElementById("notify-email") as HTMLInputElement).checked; onSave(web, email); }}>Lưu thay đổi</Button></div></Dialog>; }
